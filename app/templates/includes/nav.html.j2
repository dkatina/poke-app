<div class="container">
    <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{url_for('pokemon.index')}}">PokeDex</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-link" href="{{url_for('pokemon.index')}}">Home</a>
            <a class="nav-link" href="{{url_for('pokemon.finder')}}">Poke Finder</a>
            {% if current_user.is_anonymous %}
            <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
            <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
            {% endif%}

            {%if current_user.is_authenticated%}
            <a class="nav-link" href="{{url_for('pokemon.my_team')}}">My Team</a>
            <a class="nav-link" href="{{url_for('auth.edit_user')}}">Edit User</a>
            <a class="nav-link" href="{{url_for('auth.logout')}}">Logout</a>
            <a class="nav-link" href="{{url_for('pokemon.browse_trainers')}}">Browse Trainers</a>
            {% endif%}

        </div>
        </div>
    </div>
    </nav>
</div>
